# Python If Elif Else


Write if, else if, and else statement like this

```
if unit_price < 20
	total_price = unit_price + 1
elif unit_price >= 20 and unit_price <= 30
	total_price = unit_price + 5
else unit_price > 30
	total_price = unit_price + 10

```

